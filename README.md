# test-2
mofobigbongo

## Digital Rain

Run the digital rain effect in your terminal:

```bash
python3 digital_rain.py
```

Use `Ctrl+C` to exit the animation. If your terminal does not fully
support curses, some characters may not display correctly, but the
program will handle these errors gracefully.
